[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "krip-crypto"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
where = ["DS/src"]
